# ========================================
# 清玖成长网站 - 环境变量配置说明
# ========================================

## 🔧 必需配置（必须配置才能运行）

### 1. DATABASE_URL - 数据库连接（必需）
**用途**: Prisma ORM连接PostgreSQL数据库

**格式**:
```
DATABASE_URL="postgresql://用户名:密码@主机:端口/数据库名?schema=public"
```

**当前配置**:
```
DATABASE_URL="postgresql://openaigc:odysseywarsaw@192.168.1.16:5432/qingjiu_growth?schema=public"
```

**说明**:
- 连接到RTX-3090设备的PostgreSQL数据库
- 数据库名: qingjiu_growth
- 用户名: openaigc
- 密码: odysseywarsaw
- 主机: 192.168.1.16（内网）
- 端口: 5432

**生产环境建议**:
- 使用环境变量管理平台（如Vercel的环境变量设置）
- 不要在代码中硬编码数据库密码
- 定期更换数据库密码
- 限制数据库访问IP

---

## 📦 可选配置（增强功能）

### 2. REDIS_URL - Redis缓存（可选）
**用途**: 缓存、会话存储、队列

**格式**:
```
REDIS_URL="redis://:密码@主机:端口"
```

**当前配置**:
```
REDIS_URL="redis://:odysseywarsaw@192.168.1.16:6379"
```

**说明**:
- 用于API响应缓存
- 存储用户会话（如果添加登录功能）
- 队列管理（如果添加后台任务）

**当前状态**: 未使用，未来可选

---

### 3. NEXTAUTH_URL - NextAuth配置（可选）
**用途**: 如果添加用户登录功能

**格式**:
```
NEXTAUTH_URL="http://localhost:3000"  # 本地开发
NEXTAUTH_URL="https://your-domain.com"  # 生产环境
```

**当前状态**: 未使用，未来可选

---

### 4. NEXTAUTH_SECRET - NextAuth密钥（可选）
**用途**: 加密用户会话

**生成方法**:
```bash
openssl rand -base64 32
```

**当前状态**: 未使用，未来可选

---

### 5. OLLAMA_BASE_URL - 本地LLM推理（可选）
**用途**: 连接到本地Ollama服务

**格式**:
```
OLLAMA_BASE_URL="http://192.168.1.16:11434"
```

**说明**:
- 用于本地AI功能（如智能问答）
- RTX-3090设备上的Ollama服务
- 当前未使用，未来可选

---

## 🚀 部署环境配置

### Vercel部署
1. 登录Vercel Dashboard
2. 进入项目Settings → Environment Variables
3. 添加以下变量:
   - `DATABASE_URL`（必需）
   - 其他可选变量根据需要添加

### 本地开发
1. 复制 `.env.example` 为 `.env`
2. 根据实际情况修改配置
3. 运行 `npm run dev`

---

## 🔒 安全建议

1. **永远不要**把 `.env` 文件提交到Git
2. 使用强密码
3. 限制数据库访问IP（生产环境）
4. 定期更换密钥和密码
5. 使用不同的开发和生产环境凭证

---

## 📝 完整.env示例

```bash
# 数据库（必需）
DATABASE_URL="postgresql://openaigc:odysseywarsaw@192.168.1.16:5432/qingjiu_growth?schema=public"

# Redis（可选）
REDIS_URL="redis://:odysseywarsaw@192.168.1.16:6379"

# NextAuth（可选，如果添加登录功能）
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-generated-secret-key"

# 环境
NODE_ENV="development"

# Ollama（可选）
OLLAMA_BASE_URL="http://192.168.1.16:11434"
```

---

## ✅ 当前状态

**已使用**: DATABASE_URL
**未使用**: REDIS_URL, NEXTAUTH_URL, NEXTAUTH_SECRET, OLLAMA_BASE_URL

**可以运行的最小配置**:
```bash
DATABASE_URL="postgresql://openaigc:odysseywarsaw@192.168.1.16:5432/qingjiu_growth?schema=public"
```

---

_清玖成长网站环境配置说明_
_最后更新: 2026-03-11_
